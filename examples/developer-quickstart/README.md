# Developer quickstart example

This folder is the representative deck for the independently installable
developer path. Follow [the quickstart](../../docs/quickstart.md). Do not
treat this checkout as the install source: the documented workflow installs
published npm packages into a fresh Node 24 project.
