# Quote consumer integration checkpoint

The coordinated candidate now implements shared composition/preview/export quote geometry. All work uses branch `codex/shared-quote-integration-20260909` in the four public repositories. Core, renderer and PPTX retain their published manifest versions until release preparation; these branches are source candidates, not registry packages.

Portable source checkpoints (fetch from GitHub; no old-machine artifacts required):

| Repository | Tested implementation commit |
| --- | --- |
| OpenPresentation/opf | `7b8633a27a9b1fda9daefb3f57cd1b6a4334090d` |
| OpenPresentation/opf-render | `7f08cf9e6db1f5da58b96ca1625d9ae2614ea59b` |
| OpenPresentation/opf-pptx | `829103b67fdc96ae977bbb05f3e94457620645cd` |
| OpenPresentation/opf-editor | `a143ee7b1c06b6cb7fbca482927661ca2b208226` |

[Source-bound browser/native summary](../evidence/shared-quote-integration-2026-09-09.json), [41-slide raster review](https://github.com/OpenPresentation/opf-render/blob/7f08cf9e6db1f5da58b96ca1625d9ae2614ea59b/docs/evidence/shared-quote-raster-review.json), and [native report/contact sheets](https://github.com/OpenPresentation/opf-pptx/tree/829103b67fdc96ae977bbb05f3e94457620645cd/docs/evidence/shared-quote-integration) are committed. Native scripts in that converter commit regenerate the twelve local fixtures using Windows Calibri; font bytes are never committed. Clone these repositories as siblings, install each locked dependency set, build core, then use `node scripts/link-ecosystem.mjs --packages-only`. The editor browser harness also needs its installed `@openpresentation/opf-pptx` linked to the converter candidate; the generic linker currently links core/renderer only. Extend and test that setup as part of the next coordinator gate, rather than accidentally testing an older registry converter.

`quote-flow-v1` allocates source space before shrinking or rearranging: fixed 18px insets/gap, preferred 40px footer, at most nominal/floor footer trials, least total font reduction among fitting trials. `grid-score-v2` measures both quote parts, charges one overflow penalty per leaf, and retains explicit modes, weights, source order and parent-before-child search. Accepted fits use rounded boxes and are reused by renderer/PPTX without another fit/style pass. Strict descendant-path failures and tiny unusable boxes reject explicitly. Source content is unchanged; `item.text` aliases the body fit including generated quotation marks, while `sources` maps original text offsets.

Pagination persists evaluated readability floors and retains exact quote-body fragments, metadata and repeated attribution/source. An empty-body quote with an irreducible source after earlier content now rejects instead of being dropped. The editor records a one-page policy change in undo history; a repeated unchanged operation is a no-op.

Local evidence: all 433 core node tests and preservation suites pass on Node 20/24; renderer and PPTX full suites pass on both, including 126 decks/805 slides. All 41 changed renderer raster entries were visually compared with published renderer 0.5.1 using the same corpus/font policy: no outer content boxes move, footer allocation/font fitting changes, and preexisting contrast, placeholder and rich-text-spacing issues remain. The renderer candidate baseline is reviewed; the core published-registry baseline and actual-release refs remain unchanged.

Edge 152 checks ten wide/portrait quote cases with four loaded open font faces, complete normalized body/source text, glyph cell containment and body/footer separation. Glyph overhang into the inset reaches approximately 1px horizontally and 0.26px vertically beyond the advance-based part box; these observations are reported, not hidden with a line-box equivalence claim. Five converter browser suites pass. Twelve local Calibri PowerPoint 16 fixtures verify accepted lines/sizes, native cell containment/separation, title edits/save/reopen, six valid deck imports and all footer lines; raster differences are observations, not pixel-equivalence thresholds.

Editor integration starts from fetched main `ccd42d9276f7e47a545205469dc2665e03c28a8f`. Its offline browser harness covers source editing/undo, one-page readability preview/undo, native export and text reimport/undo. OOXML reimport currently flattens quotes into editable text blocks and does not restore the OPF quote structure, font scheme or readability policy. Preserve OPF as the authoring source; semantic/layout round-trip remains a goal gap.

Core/CLI typechecks, downstream metadata/syntax checks, all six skill checks and text integrity pass. CLI Node 24 passes 11 node tests and 69 command checks; Windows file-symlink rejection is explicitly skipped without that privilege and remains covered by Unix CI.

Remaining gates: finish the coherent four-repository source/packed coordinator and packed CLI checks, update its immutable source refs and separate candidate/published raster baselines, then run remote CI and review. Do not point actual registry verification refs at unpublished tests. No candidate PR is opened yet; no candidate is merged, published or deployed. Broader repair, Auto arrange, code/metric/timeline/chart internals, original requested-font provenance, multilingual shaping, Aptos Display fallback and native macOS evidence remain unfinished.

## Original integration audit

Read-only audit after standalone core quote API PR #46. Exact downstream bases: renderer d8696224e7a99618701e641ef7573ef394489885; PPTX 383666b366b8c9b8ed8e7e72934d1d3fd0fb634d; editor e8bdedf7f80e2eab0ff7d869b9436cd2050e426d.

- Core candidate scoring must measure both body and footer, use requested-minus-fitted reductions per part and a documented payload overflow penalty, and version the changed scoring algorithm. Keep explicit modes, weights, source order and parent-before-child decisions. Code remains incomplete. Do not mislabel new behavior as grid-score-v1.
- Accepted leaf geometry should carry the complete quote result. Fit once against final rounded item boxes; renderer/export consume those styles/lines/boxes without re-fitting. Avoid both legacy body fitting and a second complete fit for the same accepted leaf. Candidate exploration is separate from acceptance.
- Strict composition currently matches diagnostic paths exactly to item paths. A quote body diagnostic uses `.text`; descendant diagnostics must be matched with a dot boundary so inherited strict settings cannot miss them. Do not let `blocks.1` capture `blocks.10`.
- Source inspection corrected the initial audit: pagination already splits quote body text at UTF-16 grapheme boundaries and repeats the attribution/source on every part. Verify exact body reassembly, metadata/mappings and all-or-nothing rejection for irreducible footer overflow; never truncate a footer or return partial pages. Persist the evaluated readability policy in returned slides so preview/export do not revert a footer to a smaller nominal size.
- Renderer `renderTextBox` already accepts `fit` and `textStyle`. `renderQuote` can consume accepted parts directly. It currently deduplicates diagnostics by code:path, so body/footer overlap reasons sharing a path need deliberate reporting. Invalid available part boxes have no fit: do not fall back to a fresh fit or silently omit source content. Explicit rendering rejection is preferable when geometry is unusable. Warning-mode text overflow can still show all retained lines with diagnostics.
- Renderer `renderResolvedSlide` already exists. Use it with a provider that throws after resolution to prove no quote re-measurement; inspect whether unrelated decoration/font operations still need a provider and scope the test to quotes.
- Converter `addSlide` loops composed items, then passes only payload/region/context to `addPayload`. Pass the accepted quote geometry through that boundary rather than recomputing. `addMeasuredPayloadText` currently always resolves/refits; add a shared-fit/style path or a separate part consumer, with no font measurement after acceptance. Native lines should preserve exact accepted positions/styles and all text, including mappings at the core boundary.
- Parent item text currently represents legacy plain quote body; decide its compatibility contract explicitly if replacing it with the shared body fit including generated quotation marks. Existing renderer ignores it for quote rendering. Audit editor selection/source offsets and public tests before changing it.
- The initial standalone `layoutQuote` retained 18px insets/40px footer. Integration now allocates that footer adaptively as described above and can raise nominal font size to honor an explicit readability floor. This is intentionally stricter than legacy fitText capping the floor at requested size. Full consumer tests cover minFontSize > 17, wide/portrait, nested groups, string shorthand, tiny cells and irreducible overflow; standalone tests also cover source-only/attribution-only forms.
- Binding already resolves font-family substitutions before composition, so the primitive's requestedStyle may see a pre-resolved family. Broader original-requested-font provenance is still a font-roadmap requirement; do not overstate the standalone part field as preserving the entire original design request through all engines.
- Test complete local/packed/candidate cross-repository behavior before advancing source CI refs. Actual registry verification refs remain on published packages. Keep integration drafts unpublished until coherent core/renderer/converter checks, real browser bounds and native PowerPoint fixtures pass. Full layout repair, code/metric/timeline/chart internals, font shaping and macOS checks remain separate unfinished requirements.
