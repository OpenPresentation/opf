# Color schemes: 14 values

Classification: **partial** 14

Schema-valid 14/14. Catalog id resolves in core 14/14.

## Top reasons

| count | reason |
|---|---|
| 14 | scheme colours exported as literal RGB, not schemeClr |
| 14 | re-import loses colorScheme id silently |
| 13 | theme clrScheme matches 2/12 slots (..) |
| 1 | theme clrScheme matches 4/12 slots (..) |

| id | valid | catalog | engine effect | reasons | class |
|---|---|---|---|---|---|
| black-and-white | true | true | preview+export apply scheme colours (literal srgbClr) | theme clrScheme matches 2/12 slots (Office default); scheme colours exported as literal RGB, not schemeClr; re-import loses colorScheme id silently | partial |
| bold-red | true | true | preview+export apply scheme colours (literal srgbClr) | theme clrScheme matches 2/12 slots (Office default); scheme colours exported as literal RGB, not schemeClr; re-import loses colorScheme id silently | partial |
| boost | true | true | preview+export apply scheme colours (literal srgbClr) | theme clrScheme matches 2/12 slots (Office default); scheme colours exported as literal RGB, not schemeClr; re-import loses colorScheme id silently | partial |
| burnt-orange | true | true | preview+export apply scheme colours (literal srgbClr) | theme clrScheme matches 2/12 slots (Office default); scheme colours exported as literal RGB, not schemeClr; re-import loses colorScheme id silently | partial |
| cool-horizon | true | true | preview+export apply scheme colours (literal srgbClr) | theme clrScheme matches 2/12 slots (Office default); scheme colours exported as literal RGB, not schemeClr; re-import loses colorScheme id silently | partial |
| corporate-blue | true | true | preview+export apply scheme colours (literal srgbClr) | theme clrScheme matches 4/12 slots (Office default); scheme colours exported as literal RGB, not schemeClr; re-import loses colorScheme id silently | partial |
| deep-purple | true | true | preview+export apply scheme colours (literal srgbClr) | theme clrScheme matches 2/12 slots (Office default); scheme colours exported as literal RGB, not schemeClr; re-import loses colorScheme id silently | partial |
| forest-green | true | true | preview+export apply scheme colours (literal srgbClr) | theme clrScheme matches 2/12 slots (Office default); scheme colours exported as literal RGB, not schemeClr; re-import loses colorScheme id silently | partial |
| golden-yellow | true | true | preview+export apply scheme colours (literal srgbClr) | theme clrScheme matches 2/12 slots (Office default); scheme colours exported as literal RGB, not schemeClr; re-import loses colorScheme id silently | partial |
| luxury | true | true | preview+export apply scheme colours (literal srgbClr) | theme clrScheme matches 2/12 slots (Office default); scheme colours exported as literal RGB, not schemeClr; re-import loses colorScheme id silently | partial |
| pastel-red | true | true | preview+export apply scheme colours (literal srgbClr) | theme clrScheme matches 2/12 slots (Office default); scheme colours exported as literal RGB, not schemeClr; re-import loses colorScheme id silently | partial |
| slate-gray | true | true | preview+export apply scheme colours (literal srgbClr) | theme clrScheme matches 2/12 slots (Office default); scheme colours exported as literal RGB, not schemeClr; re-import loses colorScheme id silently | partial |
| steel-blue | true | true | preview+export apply scheme colours (literal srgbClr) | theme clrScheme matches 2/12 slots (Office default); scheme colours exported as literal RGB, not schemeClr; re-import loses colorScheme id silently | partial |
| vibes | true | true | preview+export apply scheme colours (literal srgbClr) | theme clrScheme matches 2/12 slots (Office default); scheme colours exported as literal RGB, not schemeClr; re-import loses colorScheme id silently | partial |
