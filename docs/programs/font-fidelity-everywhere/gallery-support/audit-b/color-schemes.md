# Color schemes: 14 values

Classification: **broken** 14

Schema-valid 14/14. Catalog id resolves in core 14/14.

## Top reasons

| count | reason |
|---|---|
| 14 | theme clrScheme matches 12/12 slots (..) |
| 14 | scheme colours exported as literal RGB, not schemeClr |
| 14 | re-import loses colorScheme id silently |

| id | valid | catalog | engine effect | reasons | class |
|---|---|---|---|---|---|
| black-and-white | true | true | colour mismatch | theme clrScheme matches 12/12 slots (Office default); scheme colours exported as literal RGB, not schemeClr; re-import loses colorScheme id silently | broken |
| bold-red | true | true | colour mismatch | theme clrScheme matches 12/12 slots (Office default); scheme colours exported as literal RGB, not schemeClr; re-import loses colorScheme id silently | broken |
| boost | true | true | colour mismatch | theme clrScheme matches 12/12 slots (Office default); scheme colours exported as literal RGB, not schemeClr; re-import loses colorScheme id silently | broken |
| burnt-orange | true | true | colour mismatch | theme clrScheme matches 12/12 slots (Office default); scheme colours exported as literal RGB, not schemeClr; re-import loses colorScheme id silently | broken |
| cool-horizon | true | true | colour mismatch | theme clrScheme matches 12/12 slots (Office default); scheme colours exported as literal RGB, not schemeClr; re-import loses colorScheme id silently | broken |
| corporate-blue | true | true | colour mismatch | theme clrScheme matches 12/12 slots (Office default); scheme colours exported as literal RGB, not schemeClr; re-import loses colorScheme id silently | broken |
| deep-purple | true | true | colour mismatch | theme clrScheme matches 12/12 slots (Office default); scheme colours exported as literal RGB, not schemeClr; re-import loses colorScheme id silently | broken |
| forest-green | true | true | colour mismatch | theme clrScheme matches 12/12 slots (Office default); scheme colours exported as literal RGB, not schemeClr; re-import loses colorScheme id silently | broken |
| golden-yellow | true | true | colour mismatch | theme clrScheme matches 12/12 slots (Office default); scheme colours exported as literal RGB, not schemeClr; re-import loses colorScheme id silently | broken |
| luxury | true | true | colour mismatch | theme clrScheme matches 12/12 slots (Office default); scheme colours exported as literal RGB, not schemeClr; re-import loses colorScheme id silently | broken |
| pastel-red | true | true | colour mismatch | theme clrScheme matches 12/12 slots (Office default); scheme colours exported as literal RGB, not schemeClr; re-import loses colorScheme id silently | broken |
| slate-gray | true | true | colour mismatch | theme clrScheme matches 12/12 slots (Office default); scheme colours exported as literal RGB, not schemeClr; re-import loses colorScheme id silently | broken |
| steel-blue | true | true | colour mismatch | theme clrScheme matches 12/12 slots (Office default); scheme colours exported as literal RGB, not schemeClr; re-import loses colorScheme id silently | broken |
| vibes | true | true | colour mismatch | theme clrScheme matches 12/12 slots (Office default); scheme colours exported as literal RGB, not schemeClr; re-import loses colorScheme id silently | broken |
