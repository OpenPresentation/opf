# Narratives: 10 values

Classification: **authoring-metadata** 1, **gallery-only** 9

Schema-valid 10/10. Catalog id resolves in core 1/10.

## Top reasons

| count | reason |
|---|---|
| 10 | consumed only by validator/lint/bundle catalog checks, opf-editor transfer mapping and authoring skills; re-import drops it |
| 9 | gallery id not in core narratives catalog (..) |

| id | valid | catalog | engine effect | reasons | class |
|---|---|---|---|---|---|
| problem-solution | true | true | no effect on preview or export (byte-identical when removed) | consumed only by validator/lint/bundle catalog checks, opf-editor transfer mapping and authoring skills; re-import drops it | authoring-metadata |
| heros-journey | true | false | no effect on preview or export (byte-identical when removed) | gallery id not in core narratives catalog (validator + lint warn); consumed only by validator/lint/bundle catalog checks, opf-editor transfer mapping and authoring skills; re-import drops it | gallery-only |
| what-so-what-now-what | true | false | no effect on preview or export (byte-identical when removed) | gallery id not in core narratives catalog (validator + lint warn); consumed only by validator/lint/bundle catalog checks, opf-editor transfer mapping and authoring skills; re-import drops it | gallery-only |
| situation-complication-resolution | true | false | no effect on preview or export (byte-identical when removed) | gallery id not in core narratives catalog (validator + lint warn); consumed only by validator/lint/bundle catalog checks, opf-editor transfer mapping and authoring skills; re-import drops it | gallery-only |
| star-method | true | false | no effect on preview or export (byte-identical when removed) | gallery id not in core narratives catalog (validator + lint warn); consumed only by validator/lint/bundle catalog checks, opf-editor transfer mapping and authoring skills; re-import drops it | gallery-only |
| pyramid-principle | true | false | no effect on preview or export (byte-identical when removed) | gallery id not in core narratives catalog (validator + lint warn); consumed only by validator/lint/bundle catalog checks, opf-editor transfer mapping and authoring skills; re-import drops it | gallery-only |
| sparkline | true | false | no effect on preview or export (byte-identical when removed) | gallery id not in core narratives catalog (validator + lint warn); consumed only by validator/lint/bundle catalog checks, opf-editor transfer mapping and authoring skills; re-import drops it | gallery-only |
| data-story | true | false | no effect on preview or export (byte-identical when removed) | gallery id not in core narratives catalog (validator + lint warn); consumed only by validator/lint/bundle catalog checks, opf-editor transfer mapping and authoring skills; re-import drops it | gallery-only |
| change-story | true | false | no effect on preview or export (byte-identical when removed) | gallery id not in core narratives catalog (validator + lint warn); consumed only by validator/lint/bundle catalog checks, opf-editor transfer mapping and authoring skills; re-import drops it | gallery-only |
| vision-roadmap | true | false | no effect on preview or export (byte-identical when removed) | gallery id not in core narratives catalog (validator + lint warn); consumed only by validator/lint/bundle catalog checks, opf-editor transfer mapping and authoring skills; re-import drops it | gallery-only |
