# Audiences: 14 values

Classification: **gallery-only** 12, **authoring-metadata** 2

Schema-valid 14/14. Catalog id resolves in core 2/14.

## Top reasons

| count | reason |
|---|---|
| 14 | consumed only by validator/lint/bundle catalog checks, opf-editor transfer mapping and authoring skills; re-import drops it |
| 12 | gallery id not in core audiences catalog (..) |
| 12 | snippet narrative (..) not in core catalog |

| id | valid | catalog | engine effect | reasons | class |
|---|---|---|---|---|---|
| executive | true | false | no effect on preview or export (byte-identical when removed) | gallery id not in core audiences catalog (no validator/lint warning); snippet narrative (recommendedNarratives[0]) not in core catalog; consumed only by validator/lint/bundle catalog checks, opf-editor transfer mapping and authoring skills; re-import drops it | gallery-only |
| investor | true | false | no effect on preview or export (byte-identical when removed) | gallery id not in core audiences catalog (no validator/lint warning); snippet narrative (recommendedNarratives[0]) not in core catalog; consumed only by validator/lint/bundle catalog checks, opf-editor transfer mapping and authoring skills; re-import drops it | gallery-only |
| board | true | true | no effect on preview or export (byte-identical when removed) | snippet narrative (recommendedNarratives[0]) not in core catalog; consumed only by validator/lint/bundle catalog checks, opf-editor transfer mapping and authoring skills; re-import drops it | authoring-metadata |
| technical | true | false | no effect on preview or export (byte-identical when removed) | gallery id not in core audiences catalog (no validator/lint warning); snippet narrative (recommendedNarratives[0]) not in core catalog; consumed only by validator/lint/bundle catalog checks, opf-editor transfer mapping and authoring skills; re-import drops it | gallery-only |
| sales | true | false | no effect on preview or export (byte-identical when removed) | gallery id not in core audiences catalog (no validator/lint warning); consumed only by validator/lint/bundle catalog checks, opf-editor transfer mapping and authoring skills; re-import drops it | gallery-only |
| marketing | true | false | no effect on preview or export (byte-identical when removed) | gallery id not in core audiences catalog (no validator/lint warning); snippet narrative (recommendedNarratives[0]) not in core catalog; consumed only by validator/lint/bundle catalog checks, opf-editor transfer mapping and authoring skills; re-import drops it | gallery-only |
| academic | true | false | no effect on preview or export (byte-identical when removed) | gallery id not in core audiences catalog (no validator/lint warning); snippet narrative (recommendedNarratives[0]) not in core catalog; consumed only by validator/lint/bundle catalog checks, opf-editor transfer mapping and authoring skills; re-import drops it | gallery-only |
| internal-team | true | false | no effect on preview or export (byte-identical when removed) | gallery id not in core audiences catalog (no validator/lint warning); snippet narrative (recommendedNarratives[0]) not in core catalog; consumed only by validator/lint/bundle catalog checks, opf-editor transfer mapping and authoring skills; re-import drops it | gallery-only |
| customer | true | false | no effect on preview or export (byte-identical when removed) | gallery id not in core audiences catalog (no validator/lint warning); snippet narrative (recommendedNarratives[0]) not in core catalog; consumed only by validator/lint/bundle catalog checks, opf-editor transfer mapping and authoring skills; re-import drops it | gallery-only |
| general-public | true | false | no effect on preview or export (byte-identical when removed) | gallery id not in core audiences catalog (no validator/lint warning); snippet narrative (recommendedNarratives[0]) not in core catalog; consumed only by validator/lint/bundle catalog checks, opf-editor transfer mapping and authoring skills; re-import drops it | gallery-only |
| media | true | false | no effect on preview or export (byte-identical when removed) | gallery id not in core audiences catalog (no validator/lint warning); snippet narrative (recommendedNarratives[0]) not in core catalog; consumed only by validator/lint/bundle catalog checks, opf-editor transfer mapping and authoring skills; re-import drops it | gallery-only |
| partner | true | false | no effect on preview or export (byte-identical when removed) | gallery id not in core audiences catalog (no validator/lint warning); consumed only by validator/lint/bundle catalog checks, opf-editor transfer mapping and authoring skills; re-import drops it | gallery-only |
| regulatory | true | false | no effect on preview or export (byte-identical when removed) | gallery id not in core audiences catalog (no validator/lint warning); snippet narrative (recommendedNarratives[0]) not in core catalog; consumed only by validator/lint/bundle catalog checks, opf-editor transfer mapping and authoring skills; re-import drops it | gallery-only |
| all-hands | true | true | no effect on preview or export (byte-identical when removed) | snippet narrative (recommendedNarratives[0]) not in core catalog; consumed only by validator/lint/bundle catalog checks, opf-editor transfer mapping and authoring skills; re-import drops it | authoring-metadata |
