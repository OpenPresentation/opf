# Open in Author: prospective navigation acceptance policy

The test-only candidate passed **39/39 local browser cases once, zero retries**, in **112.780048 seconds** on macOS / Node 24.21.0. The Open in Author case completed all original URL, decoded-source, clipboard, no-POST and page-error assertions. **Fresh Linux and Windows CI remain required.** This is not a product fix or an explanation of the original CI delay.

The sole source change [preregisters the normal navigation/load wait](base/source-review/navigation-policy.patch) around the same action. This explicitly changes the incidental five-second full-document-navigation deadline to the ordinary navigation class within the **unchanged 45-second whole-test budget**. All original assertions remain byte-exact; subsequent content assertions retain their default five-second budgets. There is no numeric timeout, retry, sleep, route/product/config change or removed assertion. [Independent policy review](base/review/REPORT.md) explains the distinction.

Fresh frozen install, scoped lint, build **`tL8W1j83XSdKiuTACo8zx`**, and prepared-tree typecheck passed. The [initial typecheck failure](acceptance/typecheck.log) is retained: generated `.opf-spec/opf.schema.json` was absent before the existing build prepared its 26 inputs. [Prepared-tree typecheck](acceptance/typecheck-after-build.log) then passed without source repair. The 704-unit suite and 13 standalone controls were not rerun locally because their product/test scope was unchanged and the a4eb platform run had passed them; fresh CI will run them again.

[Local acceptance](acceptance/REPORT.md), [all outcomes](acceptance/browser-outcomes.json), [commands](acceptance/commands.json), and [source identity](root-review/root-acceptance-verification.json) bind the exact candidate test and **702 unchanged regular inputs / 33 symlink targets**. Source, lockfile and config snapshots use `.txt`; generated schema/OpenAPI hashes are retained. [Root visual review](root-review/root-handoff-visual-review.json) binds the [completed Author screenshot](acceptance/results/inspector-actions-Inspecto-ddffb-urrent-draft-before-handoff/author-current-handoff.png). Handoff assertions establish semantic document preservation, not exact incoming JSON spelling or cross-route undo history.

The [original a4eb failure and policy staging](base/README.md) remain byte-identical under `base/`: Linux 39/39, Windows **38/39**, with the original failed trace, error context, screenshot, logs, diagnosis and reviews. Its later source assertions were not reached; the new local pass does not retrospectively accept that run or establish acceptable latency. The client-navigation source hazard remains a separate, unimplemented proposal.

All **103 non-trace browser outputs** and the one successful handoff trace are copied. The complete [142-output inventory](acceptance/artifact-inventory.json) retains all 39 trace hashes/CRC checks; [omissions](omissions.json) identifies the 38 passing traces left at their unchanged private paths. Both the original failed and new passing handoff trace ZIPs are present. [Provenance](provenance.json) binds original manifests, exact copies and parent-approved publication scope; no original bytes were rewritten.

Curation occurred outside the repository, with no commit, push, merge, deployment, additional browser/test run or native Office action. The parent subsequently adopted these exact copies for the test-only follow-up; [integration receipt](publication-integration.json) records the narrowly scoped whitespace attributes needed to preserve original captured bytes. Protected-preview, canonical-production, release and native compatibility gates remain separate.
