# Agent guide exporter repair — 2026-09-21

Scope: `scripts/export-agent-resources.mjs` and focused additions to existing `scripts/test-review-regressions.mjs` in `/private/tmp/opf-issue88-public-docs-20260921/openpresentation-site`. No package/version/docs-ref changes, builds, git mutations, or deployment by this agent.

## Cause and behavior

The prior exporter selected every `docs/` file and read each as UTF-8 for `llms-full.txt`. Its current pinned snapshot contains 10,240 docs files / 223,970,600 source bytes, including 6,664 PNGs, 514 PPTX files, 43 TTF fonts, 944 logs, and nested JS runtime bundles. Binary decoding grew the text response to 321,605,007 bytes (306.71 MiB); the guide index also listed every raw evidence artifact and grew to 2,214,188 bytes.

The exporter now selects complete Markdown documents outside `docs/evidence/` as guide prose, plus the six skill entrypoints. The canonical JSON schema and full-feature example remain included in full. Evidence Markdown reports are linked in a separate section. Other raw files remain available through the complete URL/bytes/SHA-256 manifest. This is a semantic content selection, with no truncation or arbitrary size threshold. Nested guides, root references, migrations, narrative plans, and existing compatibility aliases remain included.

Copying, raw URLs, manifest entries, bytes, hashes, examples, catalogs, schemas, skill companions/archive, and hosted reference route discovery are unchanged. This intentionally does not delete or relocate historical evidence.

## Current snapshot acceptance

- `llms-full.txt`: 321,605,007 → 1,209,184 bytes (1.15 MiB); 320,395,823 bytes removed.
- `llms.txt`: 2,214,188 → 24,242 bytes.
- Full guide contents verified byte-for-byte inline: 75 Markdown docs, six skill entrypoints, canonical schema, and full-feature example.
- All 57 evidence Markdown report links present.
- No evidence `Source:` sections, NULs, or UTF-8 replacement characters in generated prose.
- `opf import-data` and `name: opf-author` discovery strings retained.
- All 10,797 raw manifest entries unchanged: source digest `d4ecd9c0c0b30bb3b1f584dffa561fe2a562d4ae36c7ee99c43744d3134d4816`.
- Full text SHA-256: `81c874600b4ccf8fe9ca59afbdcf26d6441d2e3cfe603510a25d078c47373193`.

## Regression coverage and commands

Node v24.21.0 used. From the site worktree:

```sh
'/Users/michael/Library/Application Support/fnm/node-versions/v24.21.0/installation/bin/node' scripts/test-review-regressions.mjs > /private/tmp/opf-issue88-public-docs-20260921/exporter-regressions.log 2>&1
```

Exit 0. New regression fixture includes nested complete reference text, a skill entrypoint and companion, real binary bytes, a log inside/outside the evidence directory, evidence JSON, document fixture JSON, canonical schema, and full example. It verifies complete selected text, durable evidence link, excluded raw content markers, and each raw file's bytes/URL/size/SHA-256. Existing compatibility-alias and remaining site regressions also passed.

Current generated output was refreshed without a build using:

```sh
'/Users/michael/Library/Application Support/fnm/node-versions/v24.21.0/installation/bin/node' --input-type=module - <<'NODE' > /private/tmp/opf-issue88-public-docs-20260921/exporter-current.log 2>&1
import {exportAgentResources} from './scripts/export-agent-resources.mjs';
await exportAgentResources(process.cwd(), `${process.cwd()}/.opf-source`);
NODE
```

Exit 0. Generated byte counts and complete-content checks are in `exporter-current.json`; baseline breakdown is in `exporter-baseline.json`. `git diff --check` passed. Peer `changelog_cleanup` owns final docs pin, build, public browser acceptance, and PR integration.
