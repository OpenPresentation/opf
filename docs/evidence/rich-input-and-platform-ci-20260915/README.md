# Rich input and platform CI checkpoint

Editor PR18 contains implementation `90e57c3` and browser acceptance `b6e0fe96f0a9b1ddbb7a531582f39d82697660d8`. The original native textarea normalization bug is reproduced both in model tests and against the immutable pre-fix editor in actual Chromium.

Ten browser workflows pass in measured, estimated and prepared-painting modes from both checkout and fresh coordinated installed candidates. Existing coordinated package and browser checks pass; the installed browser suite has 276 assertions and eight trusted pointer/keyboard scenarios. The existing installed code audit checks 79 byte-matched runtime files and archive/lock integrity. Detailed source, consumer and test hashes and preserved initial failures are committed in [editor evidence](https://github.com/OpenPresentation/opf-editor/tree/b6e0fe96f0a9b1ddbb7a531582f39d82697660d8/docs/evidence/rich-input-line-endings-20260915).

Editor PR18 merged into main at `7b5d75f6be66c5d26d78c56bbe9ff49302755ad2` after successful CI run `34950054828`. The raw log confirms ten measured checkout, ten estimated checkout and ten measured installed workflows executed. Its first implementation commit also passed CI run 34948773824. Furniture continuation `8e184ee5145289c24ab03ec5a3130239f9ec9d89` incorporates the merge and passes its source suites, syntax checks and ten measured browser workflows. Its CI is pending separately; core coordinated CI now pins that commit and executes the installed rich-input fixture. No package is published by this checkpoint. The predecessor-only editor package test still encounters the unreleased card frameBox assertion against registry core 0.9.0; its failure remains documented rather than being reclassified as candidate acceptance.

Renderer commit `dcd77ea88313aaf43c1f5da11ac7347d7001d9c1` completed CI run [34947283538](https://github.com/OpenPresentation/opf-render/actions/runs/34947283538). Mac and Windows shaping jobs passed. Linux's overall package job failed its source and fresh-installed native variable-font gates: Source Serif 4 Roman, named SmText Bold, Fontkit measured 334.06213682353496px versus browser 334.193115234375px (absolute difference 0.13097841084004px, with the unchanged requirement strictly below 0.1px). The raw failed-step log is retained here. Prepared glyph painting and native font metrics remain separate acceptance requirements.

Logical SVG DOM ranges still supply editor caret geometry. These rich-input checks do not certify arbitrary font coverage, painted ligature-interior carets, bidi, real OS IME, native Office or a new renderer default. The existing native recovery and font-license constraints remain in effect.

## Completed renderer platform reports

Prepared glyph painting passes the same 663-case matrix on Linux, macOS and Windows using Node 24.20.0 and Chromium 153.0.8010.12: 650 supported cases match every independent RGBA channel, original glyph IDs, exact advances and source text; thirteen unsupported-character cases explicitly reject. All five slide PNG hashes agree across platforms and match the previously reviewed source images in renderer `docs/evidence/composed-glyph-paint-20260915/`. Every slide retains selected logical text and nonempty repeated header/footer ink (155/491 bright pixels). Raw reports and the compared hashes are retained here.

The Linux native variable-font report contains 356 observations per backend. Fontkit has five failures against the strict 0.1px gate, maximum 0.1346250000000282px; HarfBuzz has zero failures, maximum 0.04947705078126319px. This platform-specific result does not replace the previously retained Mac native report, where Fontkit passes and HarfBuzz has five failures. No single native-metric compatibility claim is supported across platforms. The complete Linux report is retained rather than reporting only its first failing case.

The latest Windows-owner handoff remains [comment 5673073461](https://github.com/OpenPresentation/opf/pull/71#issuecomment-5673073461); user-reviewed Office recovery is still required before further native automation. No Office or process action was taken.

Run `node docs/evidence/rich-input-and-platform-ci-20260915/verify.mjs` from this checkout to verify every retained file hash, the three complete painting matrices, equal five-slide hashes, all Linux backend observations and the executed editor CI commands offline.
