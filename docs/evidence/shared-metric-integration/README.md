# Shared metric source integration evidence

`summary.json` indexes exact source checkpoints, artifact hashes and this unfinished source milestone's scope. Browser reports fingerprint resolved JavaScript runtimes and actual font bytes. Native reports bind verifiers, runtime/reference-font hashes, generated PPTX files and the PowerPoint executable/OS. Proprietary fonts and generated PPTX archives are not included; committed harnesses regenerate the fixtures locally.

These are linked-source results, not verified candidate tarballs, registry installations or public deployments. Browser execution, native source recovery, accepted shape geometry and visual fidelity remain separate claims.

The native comparison deliberately exits nonzero: one of 36 native/SVG raster pairs fails accepted-cell visible-ink containment. Nine native character-range bounds exceed 0.1 point. All 108 original/saved/edited imports preserve exact tested source/type semantics. Representative native/SVG PNGs were inspected; vertical glyph placement differs. No native pixel equivalence or inter-part glyph collision claim is made.

`golden-diff.json` lists 85 changed slides from the same source corpus. The old baseline remains authoritative pending visual review. Package-check reports retain the renderer's nonzero result and list commands actually executed. Browser screenshots show selected source and edited text in both dimensions.

See [the active integration plan](../../plans/shared-metric-integration.md) for reproduction and remaining gates. Earlier primitive and published shared-code evidence stays separate.
