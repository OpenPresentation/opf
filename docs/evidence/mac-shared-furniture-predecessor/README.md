# Header/footer predecessor gap

Mac Node 20/24 reproduce four wide/portrait cases each at selected floors 16/32. Core returns no diagnostics; all three rendered furniture elements use 13px and both authored header/footer strings are absent from parsed PPTX text. Sources remain unchanged. The exact product graph and source documents are recorded in each report. This is a read-only serialized-output probe, not native Office evidence or a fixed implementation.

The retained verifier has the original Mac checkout paths; use equivalent checkouts at the recorded product commits and substitute only those paths to reproduce it. The new shared furniture plan remains an implementation task. The original timeline predecessor is separately retained for raster comparison.
