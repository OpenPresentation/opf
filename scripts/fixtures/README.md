# Coordinated renderer baseline

`opf-examples-png.sha256.json` identifies the current core examples for coordinated renderer CI. The renderer standalone suite retains its baseline for published core 0.4.0. Both enforce exact corpus and raster hashes; this file is selected explicitly only for coordinated source checks.

Reviewed against renderer 87663fb on 2026-09-08: 126 decks / 805 slides. Compared with the published-example baseline, only `technical/asset-source-forms.opf.json#1` changes. Its truncated eight-byte PNG signature is replaced with a complete project-authored PNG; the newly visible image was inspected at individual-slide scale. The other 804 PNG hashes are unchanged. This does not establish complete visual or PowerPoint parity.

`opf-examples-png.placeholder-geometry.sha256.json` is the coordinated packages golden for unpublished heading defaults (`composeSlide` cover recenter and the content title band). `ecosystem-ci.yml` selects it through `OPF_GOLDEN_BASELINE`. The renderer standalone suite keeps `opf-examples-png.furniture.sha256.json`. Do not copy this candidate into the renderer furniture file, and do not weaken the 0.1px accepted-text gate.

Reviewed on Node 24.21.0 with renderer `114bf596e9034c0b80a8c92df6a956a04ef18bef`, scale 0.25, and `systemFonts: false`. The corpus digest matches the furniture golden (`b95034931001cba636e3fbda6fba74c153dcb9914937aea374cab212eb21feaf`, 126 decks / 805 slides). Rendering current `origin/main` (`c38d0b42765853cc110d26b76eff007cf48df65a`) with that same runtime matches the furniture golden exactly. Against that main render, this candidate changes 764 slides and leaves 41 unchanged: 103 covers recenter the heading group (21 of those titles wrap), 644 content slides take the full unused two-line title band, and 17 promoted-region slides move only the spare that still leaves a three-row metric stack (4.92px, or 2.2px on `technical/full-feature-tour.opf.json#2`). Furniture bands do not move and no new overflow diagnostics appear. Gallery number-layout operating-model slides, including `gallery/business-functions/compliance-readiness-review.opf.json#5`, stay pixel-identical. `technical/dynamic-composition.opf.json` slides 1 and 3 share a body origin after the change; slide 3 is unchanged from main. Representative before/after rasters were inspected for covers, wrapped covers, one- and two-line content titles, the unchanged metric slide, and partial region slides. This is not PowerPoint or browser pixel parity.

Regenerate a candidate with the renderer golden test and `OPF_EXAMPLES_DIR` pointing to this checkout's examples. Inspect the changed images and source before copying the candidate here. Never update a baseline automatically to bypass a failing check. Do not weaken the 0.1px accepted-text gate to make hashes match.
