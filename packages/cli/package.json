{
  "name": "@openpresentation/cli",
  "version": "0.0.0",
  "private": true,
  "description": "Local-only OPF CLI source for validation and catalog inspection.",
  "license": "MIT",
  "type": "module",
  "bin": {
    "opf": "./dist/index.js"
  },
  "scripts": {
    "build": "tsup",
    "typecheck": "pnpm --filter @openpresentation/opf build && tsc --noEmit",
    "clean": "rm -rf dist",
    "test": "pnpm run build && node ../../scripts/run-node-tests.mjs"
  },
  "dependencies": {
    "@openpresentation/opf": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^20.19.0",
    "tsup": "^8.5.1",
    "typescript": "^5.9.3"
  }
}
